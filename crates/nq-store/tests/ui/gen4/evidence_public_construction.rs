#![allow(unreachable_code)]

use nq_runtime_dependency_authority::ResolvedControllingActivation;

fn main() {
    let _: ResolvedControllingActivation<'static> = ResolvedControllingActivation {
        fields: todo!(),
        reverification: todo!(),
        _invariant: todo!(),
    };
}
