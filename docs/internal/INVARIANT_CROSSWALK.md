# Runtime invariant crosswalk

This document is an internal engineering index, not a public ontology. It
tracks where each inherited runtime law is enforced, the hostile test that
guards it, and the current formalization status. File and test names are the
stable references; line numbers are deliberately omitted.

| Runtime invariant | Current enforcement point | Hostile test | Applicable Lean result |
|---|---|---|---|
| Shape conformance does not synthesize admissibility. | `crates/nq-profiles/src/validation.rs` performs compiled profile, coverage, subject, bounds, and forbidden-assertion checks after protocol decoding; `conformance.rs` and `host.rs` then enforce profile-specific consistency. | `profile_validation::rejects_profile_digest_drift_and_cardinality_escape` and `profile_validation::rejects_helper_owned_authority_and_nonce_overclaim`. | Not yet supplied. No Lean theorem is claimed by this implementation. |
| Provider identity does not synthesize report admission. | `crates/nq-core/src/provider_intake.rs::ProviderIdentityV1` records an NQ-derived local-provider identity; the private, non-deserializable `VerifiedProvider` is constructed only from the active `AdmissionLock`, retained execution verification, durable source/provider admissions, compiled profile, and evaluator identity. `EvidenceReport.backend` remains declared candidate provenance. `ProviderResponseInterpretationV1::Validated` means only protocol-valid candidate input and preserves provider-native refusal separately from NQ profile refusal. | `provider_intake::tests::declared_backend_identity_stays_candidate_data_and_success_is_not_admission`, `provider_intake::tests::provider_cannot_inject_nq_judgment_or_authority_fields`, `provider_intake::tests::first_submission_capability_escape_keeps_raw_bytes_and_typed_rejection`, and `nq_store::tests::provider_admission_is_derived_distinct_and_does_not_admit_a_report`. | Not yet supplied. No Lean theorem is claimed by this implementation. |
| Parsed provider evidence never replaces exact native outcome or raw custody. | `ProviderIntakeV1::from_capture` binds a pre-dispatch `ProviderAttempt` to the complete `RunCapture`; `ProviderIntakeRecordV1` retains the exact native `RunResourceOutcomeV1`, raw length/digest/bytes, timing, request/context, and typed response interpretation. `ProviderIntakeRecordV1::reopen_store_row` re-decodes every canonical document and reparses the independently loaded raw bytes. Schema-v5 validation retains schema v4's requirement that the intake capture and any `raw_submissions` bytes/digest/protocol outcome agree exactly. | `provider_intake::tests::exact_raw_refusal_and_context_are_one_validated_intake`, `provider_intake::tests::partial_timeout_bytes_remain_native_and_uninterpreted`, `provider_intake::tests::provider_finish_may_precede_nq_receive_without_reintroducing_process_locality`, `nq_store::tests::lossy_native_outcome_retains_partial_outer_bytes_without_report_admission`, `nq_store::tests::provider_interpretation_cannot_disagree_with_raw_protocol_outcome`, and archive typed-context/interpretation substitution tests. | Not yet supplied. No Lean theorem is claimed by this implementation. |
| Provider replay preserves the stored decision; it does not reevaluate under changed context. | `nq_store::provider_idempotency_key` derives one key from provider admission and NQ attempt identity. `Store::preflight_provider_intake` runs before profile admission/evaluation and compares the complete replay digest; an exact replay returns the stored canonical outcome and acknowledgment. Changed bytes, provider, request, subject, scope, vantage, profile, evaluator, capability, checkpoint, native outcome, or interpretation refuses. Live retry requires current provider admission, while historical receipt reopening grants none. | `nq_store::tests::provider_replay_is_exact_and_revocation_blocks_live_retry_not_history` and `engine::tests::admitted_provider_replay_reopens_stored_decision_without_evaluator_or_checkpoint_rerun`. | Not yet supplied. No Lean theorem is claimed by this implementation. |
| Durable provider acknowledgment means committed custody and canonical processing only. | Schema v5 retains schema v4's atomic insertion of `provider_intake_attempts`, its exact local-watcher origin, run, raw submission when present, admission/refusal, evaluations/findings/status, and `provider_intake_acknowledgments`; the store returns `DurableIntakeAcknowledgment` only after commit. The receipt binds intake/run/provider/status and intake/raw/result digests but has no success, admission, health, testimony, ratification, or authority field. The local helper does not receive it. | `nq_store::tests::admitted_report_evaluations_and_result_rollback_as_one_unit`, `archive::tests::provider_intake_and_acknowledgment_survive_backup_and_archive_reopen_exactly`, and `archive::tests::a_resealed_provider_acknowledgment_substitution_fails_closed`. | Not yet supplied. No Lean theorem is claimed by this implementation. |
| A binary digest identifies bytes; it does not qualify the implementation. | `crates/nq-core/src/identity.rs::ExecutionIdentity` records the executable, interpreter/wrapper, path-like fixed-file chain, configured cwd ancestry, and normalized startup runtime identity. `VerifiedLaunch::open_expected` compares independently admitted base identity before snapshots, then compares glibc loader/DSO discovery performed against the exact retained memfds. `VerifiedLaunch::spawn` re-hashes sealed snapshots and re-runs bounded runtime discovery immediately before `/proc/self/fd` execution. `runtime.rs` accepts only host ELF64 little-endian and known glibc loaders; recursively parses and byte-qualifies a fixed-system-path `DT_NEEDED` closure before loader execution; rejects global preload, RPATH/RUNPATH, audit/filter, custom, and unresolved paths; and requires the loader's cache-disabled result to equal that closure. Runtime/cwd ancestry must be root-owned, non-writable, and ACL-free in production. Runtime artifacts share the descriptor-derived per-artifact, aggregate-byte, and artifact-count budget but are not resident snapshots; a separate process-wide reservation caps only live sealed snapshot bytes. Qualification remains a separate `AdmissionManager::verify_opened_execution` decision binding config, profile, protocol, capabilities, and conformance. Late `dlopen`/module loading is expressly outside this identity. | `identity::tests::expected_identity_refuses_path_replacement_before_launch`, `identity::tests::expected_launch_rejects_huge_same_inode_replacement_before_snapshotting`, `identity::tests::rejects_huge_sparse_artifact_from_fstat_before_hashing`, `identity::tests::rejects_too_many_existing_fixed_argument_files`, `identity::tests::rejects_aggregate_sparse_artifact_overflow`, `identity::tests::retained_working_directory_is_pinned_but_ancestry_drift_is_refused`, `runtime::tests::dependency_object_parser_refuses_code_loading_tags`, `runtime::tests::rpath_and_runpath_are_refused_before_any_loader_call`, `runtime::tests::loader_output_must_equal_the_prequalified_closure`, `runner::tests::replaced_root_executable_path_cannot_redirect_verified_launch`, `runner::tests::replaced_script_and_env_shebang_paths_execute_only_retained_bytes`, `unix_runner::tests::persistent_restart_reuses_qualified_script_descriptor_after_path_swap`, `admission::tests::config_and_binary_drift_are_distinct`, and `admission::tests::capability_declaration_can_only_narrow`. | Not yet supplied. No Lean theorem is claimed by this implementation. |
| Helper testimony cannot share NQ storage/API custody. | `CommandConfig::execution_account` resolves to exact UID/GID fields in `ExecutionIdentity`; admission and launch re-resolve them. `nq-helper-sandbox` is the isolated unsafe boundary that clears groups/capabilities, sets all IDs, `no_new_privs`, pre-exec dumpability, rlimits, and installs an inherited seccomp policy preventing process-group/namespace escape or `CLONE_PARENT` reparenting. NQ uses `waitid(WNOWAIT)`, kills the complete group, then reaps its leader. The direct-child `PDEATHSIG` is only a hint; systemd `KillMode=control-group` is the packaged daemon-death backstop. Ordinary exec may reset dumpability, so helpers sharing one UID are explicitly one sibling trust/resource domain rather than falsely claimed isolated. Both runners apply the boundary. The Unix carrier uses a group-write-only random directory, exact-inode socket ownership transfer, and PID/UID/GID `SO_PEERCRED`. | `nq_helper_sandbox::tests::debug_child_still_has_no_capabilities_and_no_new_privileges`, `nq_helper_sandbox::tests::child_is_group_leader_with_hard_limits_and_descendants_cannot_escape`, `nq_helper_sandbox::tests::legacy_clone_parent_forms_are_denied_without_orphaning_waitable_children`, conditional distinct-identity sandbox/stdio/Unix tests, `admission::tests::capability_declaration_can_only_narrow`, and the package static-invariant test. | Not yet supplied. No Lean theorem is claimed by this implementation. |
| Staleness is loss of reliance, not negation. | `crates/nq-profiles/src/host.rs::newest_current_report` returns `CannotEvaluate` outside the freshness window; `crates/nq-core/src/engine.rs::build_finding_event` retains the current condition/evidence while changing visibility; `crates/nq-store/src/lib.rs::validate_finding_against_evaluation` forbids resolution from a refused evaluation. | `profile_validation::detector_requires_newest_current_complete_coverage_to_resolve` and `nq_store::tests::stale_refusal_retains_active_condition_and_evidence_until_explicit_resolution`. | Not yet supplied. No Lean theorem is claimed by this implementation. |
| Renewal creates a new report and basis; old evidence is never refreshed in place. | `crates/nq-store/src/schema.sql` makes admitted reports append-only and gives every renewal a new NQ-owned `report_id` and database `report_sequence`. Finding evidence references immutable report identities and semantic digests rather than mutable “last seen” rows. | `nq_store::tests::same_profile_instances_remain_distinct_and_renewal_is_immutable` attempts an in-place report update and verifies that a semantically identical renewal remains a distinct report; `engine::tests::identical_semantic_reports_keep_exact_detector_evidence_identity` proves that identical semantic bytes do not collapse two committed occurrences. | Not yet supplied. No Lean theorem is claimed by this implementation. |
| Binding mutation and collection are one serialized per-instance lifecycle, and crash recovery cannot conceal ordinary drift. | `crates/nq-core/src/coordination.rs::InstanceGuard` derives a kernel lock from canonical database path/device/inode and instance identity for every collection, freshness evaluation, test, admission, rotation, rollback, and revocation. `Store::begin_binding_transition` commits the authoritative append-only binding event and bounded materialization intent in one transaction; immutable triggers reject update/delete. The intent binds canonical admissions-root path/device/inode/mode. `CollectionEngine::reconcile_pending_binding` replays only an unfinished intent against that exact root and then appends completion. `authoritative_active_lock` requires the derived file to match the latest durable event/admission exactly. | `coordination::tests::serializes_two_processes_for_the_same_instance`, `coordination::tests::refuses_a_symlinked_coordination_directory`, `mutation_serialization::daemon_collection_and_cli_revocation_are_serialized_across_processes`, `nq_store::tests::binding_event_and_recovery_intent_are_atomic_and_survive_process_restart`, and `engine::tests::pending_authoritative_binding_is_reconciled_but_untracked_tampering_is_not`. | Not yet supplied. No Lean theorem is claimed by this implementation. |
| A checkpoint is valid only within its exact admission and semantic collection contract. | `checkpoint_contract_digest` binds the admission ID, binding digest, exact profile version/digest, subject, scope, vantage, and granted capabilities. The digest is also part of the schema-v4 provider intake/replay identity. `watcher_runs.checkpoint_contract_digest` records that namespace and `Store::latest_checkpoint` selects only profile-admitted reports whose complete provider-intake transaction and acknowledgment committed in the exact namespace; receiving provider bytes alone never advances it. A migrated-v3 checkpoint remains historical data but is ineligible for live advancement because its explicit intake gap cannot supply a v4 acknowledgment. | `checkpoint_commit::only_committed_admitted_reports_advance_the_next_request_checkpoint` proves rejected/dry candidates do not advance and that rotation starts with no inherited cursor; `nq_store::tests::checkpoint_lookup_is_isolated_by_exact_contract_digest` proves two namespaces on one instance cannot cross-read; `nq_store::tests::acknowledgment_insert_failure_rolls_back_checkpoint_and_complete_intake` proves an injected acknowledgment failure leaves no cursor or partial collection; `nq_store::tests::exact_v3_upgrade_derives_provider_authority_but_not_intake_evidence` proves a preserved v3 checkpoint is not promoted into the live v4 cursor; `engine::tests::admitted_provider_replay_reopens_stored_decision_without_evaluator_or_checkpoint_rerun` proves replay does not advance or re-evaluate the stored decision. | Not yet supplied. No Lean theorem is claimed by this implementation. |
| Report recency and finding lineage come from exact committed occurrences, not clocks or digest ordering. | SQLite allocates `admitted_reports.report_sequence`; `Store::evidence_snapshot` captures reports and high-water marks in one transaction. Detectors receive `DetectorReport { report_id, report_sequence, report }` and select recency by sequence. `engine.rs::reconstruct_detector_reports`, `FindingLineage`, and `build_finding_event` require the exact profile contract and the detector's report-ID/sequence/semantic-digest triple before persisting an evidence reference. | `profile_validation::detector_recency_uses_only_durable_report_sequence`, `engine::tests::identical_semantic_reports_keep_exact_detector_evidence_identity`, `engine::tests::changed_semantic_identity_cannot_inherit_or_relabel_a_finding`, and `engine::tests::changed_semantics_open_a_distinct_finding_or_no_finding`. | Not yet supplied. No Lean theorem is claimed by this implementation. |
| Evaluation and finding revisions are durable database order, never caller-selected counters. | `Store::commit_evaluation` allocates both the next revision for one detector ID/version and a gap-free store-wide `evaluation_sequence` inside the same immediate transaction that writes the complete `EvaluationEnvelopeV2`, its watermarks, refusal, and optional finding event. Collection-triggered evaluations use `Store::commit_admitted_collection`, which assigns the real report sequence and commits the run, custody, report, exact evaluation/finding set, and V2 run result as one transaction. Reopening requires a canonical result for every completed run and requires each admitted trigger set to reproduce the admission's exact detector-suite digest and evaluator-artifact identity. `insert_finding_event` independently allocates the next event revision from the current event for the opaque finding; uniqueness constraints reject duplicate revisions, and a rolled-back transaction consumes neither counter. `evaluation_history_bounded` freezes one inclusive `through_sequence` and pages with an exclusive cursor. | `nq_store::tests::stale_refusal_retains_active_condition_and_evidence_until_explicit_resolution` asserts committed evaluation revisions 1/2/3 and finding-event revisions 1/2/3 and proves a rejected transition consumes neither revision; `nq_store::tests::admitted_report_evaluations_and_result_rollback_as_one_unit` proves a late result failure exposes no part of an admitted chain; `nq_store::tests::historical_response_run_without_result_fails_closed`, `nq_store::tests::historical_admitted_report_without_run_result_fails_closed`, and `nq_store::tests::historical_partial_evaluation_set_cannot_reopen_as_admitted_result` reject crash-partial historical chains; `nq_store::tests::admitted_detector_suite_and_evaluator_binding_fail_atomically`, `nq_store::tests::historical_reopen_rejects_omitted_admission_detector_suite`, and `nq_store::tests::historical_reopen_rejects_substituted_admission_evaluator_artifact` reject admission-evidence omission or substitution; `engine::tests::public_evaluation_history_crosses_the_maximum_page_without_silent_truncation` proves the 1,001st append remains reachable beyond the maximum page. | Not yet supplied. No Lean theorem is claimed by this implementation. |
| Missing testimony cannot resolve a condition or create a green result. | Detectors expose `CannotEvaluate` separately from `ExplicitlyAbsent`; `crates/nq-core/src/engine.rs::build_finding_event` emits no new finding for first-ever missing testimony and preserves an existing condition under refusal. The authoritative `EvaluationEnvelopeV2` still reaches the V3 evaluation status component and immutable evaluation-history read model; the store permits `resolved` only for explicit absence with sufficient visibility. | `engine::tests::missing_testimony_does_not_create_an_absence_finding`, `profile_validation::detector_requires_newest_current_complete_coverage_to_resolve`, `profile_validation::host_detector_same_code_refusals_preserve_distinct_structured_details`, `api::tests::v3_findings_api_preserves_same_code_evaluation_refusals`, `evaluation_refusal_surfaces::same_code_evaluation_refusals_survive_backup_cli_and_cold_archive`, and `nq_store::tests::stale_refusal_retains_active_condition_and_evidence_until_explicit_resolution`. | Not yet supplied. No Lean theorem is claimed by this implementation. |
| Coarse health cannot substitute for an exact detector judgment. | `crates/nq-core/src/engine.rs::health_from_evaluation` deliberately maps both `DetectorState::Present` and `DetectorState::ExplicitlyAbsent` to operator projection `HealthState::Healthy`, while retaining distinct codes and complete `EvaluationEnvelopeV2` values. That coarse projection is not used for provider intake/admission, report admission, checkpoint advancement, testimonial sufficiency, or authority. | `engine::tests::coarse_healthy_projection_cannot_substitute_for_exact_detector_judgment` constructs both judgments with the same coarse state and proves their exact codes, states, and canonical envelopes remain non-interchangeable. | Not yet supplied. No Lean theorem is claimed by this implementation. |
| Scope, vantage, and capability may narrow but never expand at runtime. | `crates/nq-core/src/engine.rs::validate_compiled_config` invokes profile-owned binding correlation before startup; `crates/nq-protocol/src/validation.rs` checks exact request echo and capability subset; `crates/nq-profiles/src/validation.rs::validate_common` plus each module's binding/basis validation compare testimony to the NQ-owned request; admission grants only the declared/configured intersection and re-verifies the ceiling. | `engine::tests::compiled_config_enforces_profile_owned_binding_correlation`, `profile_validation::rejects_subject_scope_and_capability_escape`, protocol fixture `protocol/fixtures/invalid/response_capability_escape.ndjson`, and `admission::tests::capability_declaration_can_only_narrow`. | Not yet supplied. No Lean theorem is claimed by this implementation. |
| Refusals retain the exact responsible instance, boundary, and dependent payload through every testimonial surface. | `AcquisitionFailure`, `AdmissionRefusal`, and versioned `GovernedRefusal` feed the canonical `CollectionOutcome`; helper and profile source objects remain embedded rather than copied into a display enum. Non-admitted collection outcomes remain V1; admitted V2 outcomes embed their exact ordered evaluations. `RunResourceOutcomeV1` preserves exact acquisition testimony. Schema v5 retains schema v4's atomic binding of every new provider intake and completed run to its canonical result; when the bounded diagnostic producer applies, the exact `nq.diagnostic_execution.v1` bytes commit in that same transaction. The canonical persisted evaluation is `EvaluationEnvelopeV2`, which binds its inner `EvaluationResultV1` to trigger run, full context, detector/evaluator, profile, time, and watermark identities. V3 status and frozen evaluation history expose even a first-ever `CannotEvaluate` for which the no-finding law correctly creates no finding. Status, custody, evaluation, finding, provider-history, and diagnostic-artifact readers validate every duplicated projection and stable link. Backup and cold-archive verification exhaustively reopen these typed surfaces and the archive database in SQLite immutable mode; old v1/v2 storage and unversioned/bare rows fail closed, while migrated v3/v4 stores retain explicit absences rather than synthetic evidence. Exact conversions and coarse-field classifications are in `audit/SEMANTIC_TRANSPORT_MAP.md` and `audit/REFUSAL_PRESERVATION_CROSSWALK.md`. | The four original forcing tests remain ordinary passing regressions: `engine::tests::forcing_exchange_timeout_phase_survives_dry_and_status_surfaces`, `engine::tests::forcing_protocol_refusal_dependent_fields_survive_collection_status`, `engine::tests::forcing_profile_refusal_identity_survives_collection_status`, and `nq_store::tests::forcing_rejected_submission_requires_typed_refusal`. Same-code pairs additionally cover admission, protocol rejection, helper retry/detail, real host-detector structured details, profile identity/scope/boundary, evaluation refusal, linked custody, store backup/reopen, strict product wire decode, daemon, V3 status/API, evaluation history, structured CLI, maximum-page continuation, and cold archive in `profile_validation::host_detector_same_code_refusals_preserve_distinct_structured_details`, `engine::tests::real_host_detector_same_code_refusals_survive_governed_store_and_reopen`, `engine::tests::finding_transport_uses_only_the_exact_evaluation_context_rows`, `engine::tests::collection_outcome_wire_codec_rejects_omitted_extra_duplicate_and_substituted_fields`, `engine::tests::same_code_admission_upstream_refusal_pair_survives_status_backup_reopen`, `engine::tests::same_code_protocol_rejection_pair_survives_wire_custody_status_backup_reopen`, `engine::tests::public_evaluation_history_crosses_the_maximum_page_without_silent_truncation`, `semantic_surfaces::cli_and_cold_archive_reopen_exact_same_code_refusal_payloads`, `custody_pagination::cli_pages_same_code_refusals_without_losing_payloads`, and `evaluation_refusal_surfaces::same_code_evaluation_refusals_survive_backup_cli_and_cold_archive`. Provider-hostile tests additionally reject raw/context/interpretation/acknowledgment substitution, provider judgment/authority injection, replay conflict, stale provider admission, and synthetic v3 intake. Diagnostic-artifact tests preserve determinate and refused contract bytes through commit/restart and reject missing, corrupt, unsupported, or substituted material. | External AC-R4 remains pinned to `Admissibility.Calculus.LosslessEncoding`, `encodePacket_injective`, `distinct_refusals_encode_distinct`, `decode_encodePacket`, and `refusal_recoverable` at Lean revision `ff491b80…`. These are specification evidence only. The released pin is covered by r5; the post-release pin `44e5567…` has its own passing clean-pinned ledger with 2/2 biting mutations. No formal proof claim is inferred from Rust tests. |
| Detector-refusal producer semantics and admitted evaluation order are canonical testimony, not reconstructible projections. | `EvaluationEnvelopeV2::validate` requires a `CannotEvaluate` result to retain the producer's `Detector` boundary, `CannotEvaluate` code, exact message/summary equality, and empty affirmative evidence. `validate_admitted_result_document` and core historical reopening compare the carrier sequence directly with ascending durable `evaluation_sequence`; neither side is sorted into an unordered set. | `engine::tests::evaluation_envelope_rejects_substituted_cannot_evaluate_boundary`, `engine::tests::evaluation_envelope_rejects_substituted_cannot_evaluate_code`, `engine::tests::evaluation_envelope_rejects_substituted_cannot_evaluate_message`, `engine::tests::evaluation_envelope_rejects_evidence_on_cannot_evaluate`, and `nq_store::tests::historical_reopen_rejects_reordered_admitted_evaluation_sequence`. | The AC-R4 injectivity laws apply to the complete ordered carrier. Runtime correspondence is evidenced by the passing r5 ledger at clean pin `2c41b0a`; no formal proof claim is inferred. |
| Multi-witness findings use explicit detector composition, never consensus-by-counting. | The compiled `ProfileModule::detectors` registry is the only detector entry point. `crates/nq-core/src/engine.rs::CollectionEngine::evaluate_instance` supplies an explicit instance-scoped report set and no generic quorum/counting path exists. | `profile_validation::detector_requires_newest_current_complete_coverage_to_resolve` proves that testimony from a different instance cannot shadow the bound instance. A positive compiled multi-witness detector and its hostile composition corpus are not yet supplied. | Not yet supplied. No Lean theorem is claimed by this implementation. |
| A diagnostic-artifact commitment is not its locator, import event, availability state, producer authentication, reliance, or authority. | Schema v5 separates the contract-owned artifact ID from the full canonical-byte digest, append-only commitment, optional exact payload, exclusive local/import origin, and append-only import events. `CollectionEngine::diagnostic_execute` commits the artifact with its run/evaluation and then uses `reopen_diagnostic_artifact`; read-only `diagnostics qualify` emits a content-identified admission-provenance carrier only after complete local v2 history reopens, `diagnostics inspect` exposes typed support and byte state, and exact export refuses unavailable or corrupt bytes. Import validates physical-file bounds and canonical bytes, strictly validates supported semantics, may retain unknown schemas only as unsupported custody, and explicitly grants no producer authentication, reliance, or authority. Qualification establishes evidence eligibility only and refuses imported custody. | `nq_store::tests::diagnostic_artifact_import_preserves_commitment_availability_and_corruption_states`, `engine::tests::diagnostic_execute_emits_exact_artifact_from_committed_host_evaluation` (including imported-custody refusal), the governed-refusal and acquisition-failure diagnostic tests, `engine::tests::diagnostic_execute_preserves_a_live_detector_refusal`, `archive::tests::diagnostic_artifact_availability_and_schema_support_survive_archive`, CLI parser tests, and the Campaign 1 restart/export/import transcript. | Not yet supplied. No Lean theorem is claimed by this implementation. |
| A schema version names one complete compiled SQLite definition, not merely an object inventory. | `Store::validate` checks application/version identity, required objects, and a SHA-256 fingerprint over the type, name, owning table, and SQL definition of every non-internal `sqlite_schema` entry. The current definition is schema v5. The exact released v3 and immediate v4 definitions are frozen separately; `upgrade_v3_to_v4` and `upgrade_v4_to_v5` accept only those fingerprints, create verified backups, and apply their additive migrations transactionally. | `nq_store::tests::schema_shape_and_backup_are_verified` replaces a required public view under the same name and proves that the definition fingerprint refuses it; store migration tests cover exact v4-to-v5 behavior, and `admin_lifecycle` tests exercise exact v3-to-v4-to-v5 chaining and reject modified sources. | Not yet supplied. No Lean theorem is claimed by this implementation. |
| Historical v3 runs are not upgraded into evidence they never recorded. | `Store::upgrade_v3_to_v4` may derive a prospective `local_provider_admissions` contract from exact source-admission facts, recording source admission time separately from derivation time/kind. It creates one `legacy_v3_watcher_run_intake_gaps` record for every old watcher run and no `provider_intake_attempts`, raw capture, idempotency identity, or acknowledgment. Current validation requires every run to have exactly one real v4 intake/acknowledgment or one explicit v3 gap. | `nq_store::tests::exact_v3_upgrade_derives_provider_authority_but_not_intake_evidence`, `admin_lifecycle::exact_v3_upgrade_accepts_typed_empty_history_and_refuses_semantic_or_schema_drift`, and `archive::tests::migrated_v3_history_reopens_as_an_explicit_gap_not_a_synthetic_intake`. | Not yet supplied. No Lean theorem is claimed by this implementation. |
| Backup and restore copy one committed SQLite state, including pages still resident in WAL. | `Store::backup_verified` validates the source, uses SQLite's online backup API, opens and validates the standalone destination, hashes it, and removes partial output on failure. Core backup/restart validation exhaustively reopens typed provider intake/raw/acknowledgment/gap history as well as existing collection testimony. The CLI backup, restore, and upgrade-backup paths all use verified operations rather than copying the main database file. | `nq_app::admin_lifecycle::backup_restore_and_already_current_upgrade_are_verified_and_non_destructive` keeps a committed status event only in a live source WAL and proves the restored standalone database contains it; `archive::tests::provider_intake_and_acknowledgment_survive_backup_and_archive_reopen_exactly` proves exact provider history survives backup/cold reopen; migration tests verify the preserved v3 backup remains v3. | Not yet supplied. No Lean theorem is claimed by this implementation. |
| Declared infrastructure facts, system cuts, and consumer projections describe scope but confer no authority. | `crates/nq-system-contract/src/lib.rs` gives every draft, cut, and projection the single closed `AuthoritySemantics::None` value. `PorterActuationProjectionBodyV1` can carry only narrowed targets/components, incident dependencies, and already-authored NQ verification obligations; it has no command, effect, grant, approval, native-plan, or successful detector/result-postcondition field. Strict decoding rejects unknown fields at every typed boundary. | `nq_system_contract::tests::compiling_a_cut_and_both_projections_is_deterministic_and_authority_free` inspects the exact serialized Porter body; `strict_boundary_rejects_duplicates_unknown_fields_and_authority_overclaim` rejects both an added `authorized` field and any value other than `none`. | Not yet supplied. No Lean theorem is claimed by this implementation. |
| A published system cut is an immutable identified closure; new facts or draft edits never refresh it in place. | `SystemSpecV1::compile_cut_proposal` closes the selected system over exact source snapshots, targets, components, dependencies, and observation obligations and qualifies only that selected closure against the compiled profile catalog. `compile_cut` requires a self-consistent ratification record naming that exact schema-domain-separated proposal digest. `PublishedScopeCutV1::verify` rechecks proposal/record/cut digests, complete closure, and canonical order without consulting live fact sources or requiring a current profile; `qualify_with_compiled_profiles` separately establishes current executable compatibility. | `nq_system_contract::tests::changed_draft_creates_a_new_cut_without_refreshing_the_old_cut` proves a changed target identity and ratification produce a distinct cut while the old cut remains valid; `publication_requires_exact_compiled_profile_and_ratified_proposal` rejects profile digest/coverage/freshness drift, a record bound to other proposal bytes, and mutated ratifier fields, while proving unrelated draft profiles do not block the selected closure and immutable custody survives a retired profile; `embedded_digest_and_cut_order_are_verified` rejects body drift and non-canonical reorderings. | Not yet supplied. No Lean theorem is claimed by this implementation. |
| Observation and actuation consumers may narrow an exact published cut but cannot silently expand it or substitute another cut. | Every projection carries the exact `cut_id` and `cut_digest`. NQ projection verification reconstructs the complete observation projection from that cut. Porter projection compilation requires the direct target set to equal the direct components' hosts, derives transitive dependency consumers as a separate affected scope, and includes every cut-authored witness obligation for that affected scope. Those obligations require testimony and are not mislabeled as successful detector/result postconditions. | `nq_system_contract::tests::porter_projection_separates_actuation_from_affected_verification_scope` exercises legal direct narrowing, provider-to-consumer affected expansion, unknown-component escape, unused-target expansion, and automatic inclusion of multiple witness obligations; `changed_draft_creates_a_new_cut_without_refreshing_the_old_cut` proves an old projection refuses a replacement cut. | Not yet supplied. No Lean theorem is claimed by this implementation. |

## Audit rule

An invariant is not “formally proved” merely because a Rust test exists, and a
binary/profile digest is never substituted for a qualification result. When a
relevant Lean theorem is actually supplied, this crosswalk must identify its
exact declaration and the assumptions connecting it to the runtime enforcement
point.
