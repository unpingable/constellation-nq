# Native Continuity memory support (CLASSIC-RETIREMENT)

This compiled projection family replaces the identified classic
`continuity_rely_eligible` source dependency. That claim concerns the Continuity
MEMORY rely gate, not Standing substrate-incarnation succession. Existing modern
Standing/NQ continuity and substrate-origin carriers explicitly do not establish
routine reliance and cannot substitute for this source.

Source contract: `continuity.rely_export.v0`, maintained Continuity revision
`aed09d3f1833f893771d7e031f83f76dbc5900ff`, `src/continuity/rely_export.py` and
`contctl rely-export MEMORY --evaluation-time TIME`. Actual producer refusal
is still a successful export; inability to produce a record is absence/error,
not fabricated eligible testimony. Source premises, authoring tier, effective
reliance ceiling, lifecycle and nonclaims are retained verbatim.

`nq continuity-support --source-export FILE --binding FILE` invokes the compiled
native projection. Binding pins raw source bytes, source store/memory/scope,
primary subject/scope digest, principal `nightshift-readonly-continuity` and
purpose `continue_observing`. Replay recomputes the entire native receipt.
Source export IDs remain source disclosures; NQ's own SHA/JCS receipt identity
does not promote them to authenticated custody. Normal acquisition uses the
actual configured contctl producer; arbitrary source-file authenticity is not
inferred from self-consistent hashes.

Native `nq.continuity-memory-support/v1` means exactly: the retained external
source export reports the named rely result at its recorded evaluation time.
Eligible is not world truth, source-currentness, premise discharge, authority,
or permission to act. Uncommitted/missing-premise inability remains indeterminate;
revocation/noneligibility remains distinct. Contradictory source code/boolean
or lifecycle, unknown closed-schema fields, recursive NQ substitutions, wrong
store/memory/scope/principal/purpose, changed raw bytes and changed receipts
refuse. No classic executable or classic fixture regenerator is invoked.

The purpose-support request carries this exact native qualification separately
from locally admitted diagnostic artifacts. It can close the named HISTORICAL
continuity prerequisite only for the exact primary subject/principal/purpose
and bounded citation time. Nightshift then binds both primary artifact identity
and this native qualification identity into the existing PresentEvidencePort
live query. Actual qualified present support supplies currentness on its own
receiver clock; source export/evaluation wall time cannot do so. No new clock,
authority service, workflow layer or Standing substitution is introduced.

Qualification uses actual contctl on campaign-owned SQLite for eligible,
revoked and uncommitted exports, native NQ compiled projection/replay and real
local NQ diagnostic history, then Nightshift's existing qualified-port boundary
fixtures for current/absent/unknown/expired/wrong-subject/wrong-purpose outcomes.
The port fixture is not a production Pulse deployment claim. Historical M2
acceptance remains on its original revisions.
