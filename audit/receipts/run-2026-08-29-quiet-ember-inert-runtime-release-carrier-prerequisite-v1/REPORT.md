# QUIET-EMBER inert runtime release carrier prerequisite

## Classification

`QUALIFIED-PREREQUISITE — INERT RUNTIME RELEASE CARRIER V1`

This result qualifies only the inert OCI hold/release carrier prerequisite. It grants no NQ, Docket, AG, Kubernetes, provider, deployment, or production authority and does not start BEDROCK B6 or OPEN-QUARRY.

## Lineage

- canonical retained NQ source base: `7c361a9b43c6eb25645dcde5fa5222ae5a70ac28`
- BEDROCK entry-gate refusal evidence: `5fc7cc8212eb182bba54a2c92b7b37bd44b0cf69`
- clean-source implementation commit used for the OCI specimen: `35a3cc9ea956d93697e3cfc73f7a7bd3733f415a`
- campaign branch: `campaign/quiet-ember-inert-runtime-release-carrier-prerequisite-v1`

## Qualified law

The OCI default is an inert `nq-bedrock-runtime-carrier`, not `/usr/bin/nq --version`. Before a release file exists it performs no executable invocation. Admission requires exact canonical closed binding and release documents that repeat and bind occurrence, coordination domain, mechanics, runtime recheck, durable claim, executable digest, argument vector, and one-use nonce.

The carrier opens the executable with `O_NOFOLLOW`, validates regular-file identity and permission boundaries, hashes the opened object, and executes that same object through `/proc/self/fd`; pathname replacement cannot select different bytes. It persists a one-use SQLite claim with `BEGIN IMMEDIATE` before invocation. The state directory and database are exact private owned objects, final symlinks are refused, and parent/file identities are rechecked across SQLite open.

`claimed`, `in_flight`, `outcome_unknown`, and `complete` remain distinct. An inode-bound invocation lock is held across mechanics and inherited by the exact child. Reconciliation must acquire that same retained lock identity, so it cannot overtake either the carrier or a child surviving carrier SIGKILL. Completion and reconciliation each require exactly one claimed-row update. A crash/runner-I/O window is never replayed; exact evidence-bound reconciliation closes it without re-invocation.

## Deterministic qualification

Focused carrier suite: 10 cases passed (9 unit cases and 1 real local parent-death integration case), 0 failed.

- no release: inert timeout, zero invocation
- exact release: one invocation; exact replay: zero further invocation
- malformed, noncanonical, unknown-field, substituted identity, and substituted executable cases: zero invocation
- pathname replacement after opened-object validation: one bound-object call, zero unbound-object calls
- concurrent carriers: one invocation total
- runner cancellation/restart: `outcome_unknown`, zero replay invocation, exact reconciliation only
- reconciliation versus active runner: refused until live invocation finishes
- state-file symlink and non-private parent: refused before invocation
- real `/usr/bin/sleep` child: carrier SIGKILL leaves child-held fence; reconciliation refuses while child lives and succeeds only after child teardown

Strict focused clippy with `-D warnings`, formatting, shell syntax, and diff checks passed. The full workspace suite passed with zero failures (repository-declared ignored benchmarks remained ignored).

## OCI specimen

- image reference: `bedrock.local/nq@sha256:0faebb357b63c4d48ae38d25c15d166dbc89653ab961ca360f6ae64c8a4a63b1`
- config digest: `sha256:e56ae1e46c93723ca2f05fc2972837be85eb119caa09250d0dfab895613cb342`
- layer digest: `sha256:0a9765a91eb70388b48a84ae552b12ae257e0cd7e735ff5f28f1b973ed510b00`
- carrier digest: `sha256:34691c0d39574336f86128d77ddcd1475767da36f6518772ded08da3670c3b82`
- NQ digest: `sha256:41250681a714ca48f5aaedb335b0c5f54cb9849c46cd5f6e10abaed34128e4b7`
- default entrypoint: `/usr/bin/nq-bedrock-runtime-carrier`
- specimen source was clean commit `35a3cc9ea956d93697e3cfc73f7a7bd3733f415a`

The local OCI layout and archive digest list reopened exactly. The specimen was never loaded, pushed, scheduled, or run as a container.

## Authority/effect accounting

- AG proposals/spends/issuances: `0/0/0`
- Docket attempts/settlements: `0/0`
- NQ live prepared claims/fences/releases: `0/0/0/0`
- Kubernetes workloads/services/pods: `0/0/0`
- provider starts and VMs: `0/0`
- live-route/default-branch changes: `0/0`
- test executable invocations: bounded local fixtures only
- retained child processes/listeners: `0/0`

## Custody and resume point

The branch is independently promotable as one prerequisite only. GRANITE-FENCE remains a distinct successor history. RIVER-CLERK remains a distinct later adapter campaign awaiting the frozen Docket fixture requirements. GLASSHOPPER and all live routes were untouched.

The next BEDROCK live occurrence remains refused until all three prerequisites independently qualify and an authorized successor supplies exact runtime binding, durable state mount, claim/fence identity, and one-use release.
