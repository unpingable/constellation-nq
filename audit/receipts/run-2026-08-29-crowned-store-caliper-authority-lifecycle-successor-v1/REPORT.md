# CROWNED-STORE authority-lifecycle successor result

Campaign: `CROWNED-STORE`  
Slug: `crowned-store-authority-lifecycle-successor-v1`  
Classification: `PASSIVE-VM-AUTHORITY-LIFECYCLE-NOT-QUALIFIED — PROVIDER-CONFIG-CARRIER-MISMATCH-AFTER-AUTHORITY`

## Lineage and release

CROWNED-STORE is a distinct successor to terminal CALIPER and the qualified
GROUP-ANCHOR mode-contract repair. It did not reopen CALIPER C3. The exact
GROUP-ANCHOR source was `a56342762a8b46f4c01b5885c7d7c5728ab27a60`.

Two independent offline release assemblies matched byte for byte. The Debian
package SHA-256 is
`dfb8e57c6decb87647f66f1f89d0126f215541701c60143dc1166128b82e35d1`;
the tar SHA-256 is
`dd6725c8c7886a608f549d3da6dcfb459f804c312b792c8530818f3f18f64812`.

## Occurrence v1

The first fresh VM stopped before package installation or authority because
the controller required `status: done` at the beginning of the cloud-init
output line. Cloud-init emitted progress dots followed by that exact terminal
suffix. The controller powered the VM off. All authority counts were zero.
The occurrence was not resumed.

## Consumed occurrence v2

The second occurrence used a new overlay, cloud-init identity, SSH identity,
host keys, MAC address, and SMBIOS serial. Both dedicated stores inherited the
packaged parent SGID bit and reported decimal mode `1512` (`02750`), exact
observer/reader ownership, and sufficient free space. This satisfied the
closed GROUP-ANCHOR `0750 | 02750` permission contract.

The occurrence then created and activated one H grant, enrolled E1 and E2,
issued two generation and two enrollment child grants, recorded one watcher
succession relation, and produced one G1 sample. Before any recurrence
acquisition, provider start, office activation, or handoff, the first
`reconstruct-selection-index` call refused. The retained fixture wrote
`ProviderConfigV1` as JSON at `provider-g1.json`; the current helper's exact
carrier is TOML. The helper refused at line 1, column 1 with exit status 2.

This is terminal. The H/G/E occurrence is consumed and must not be replayed or
resumed. A successor must correct the carrier before authority and use a new
campaign and occurrence.

## Closeout and custody

The controller collected a key-excluding terminal evidence archive, powered
the VM off, closed loopback port 24124, and both stopped overlays passed
`qemu-img check`. No GLASSHOPPER, production route, or default branch was
touched. The repository-retained evidence contains no private signing key.

