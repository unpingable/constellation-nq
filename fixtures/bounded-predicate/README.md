# Bounded predicate migration specimens

These declarative identity/typed-input specimens preserve the actual consumer
requirements previously recorded at classic source2238b35147e0f701f919cef102778c35e8430f4e
in `fixtures/project-predicate/{profiles,specimen-profiles}.json`. They are data,
not a dependency on the classic evaluator or fixture generator. Their original
profile labels are retained identity inputs, not a claim of transferred acceptance.

The modern NQ-ng `bounded-predicate` implementation admits only its explicitly
compiled finite predicate forms. It must reject unsupported expressions instead
of interpreting an arbitrary runtime catalog. Generate new admissions and replay
results with the exact qualified modern `nq`; never reuse classic receipts as
modern evidence. Subject, producer and manifest bindings remain load-bearing.
These test subjects are fixtures, not observed deployments or product defaults.

`queue-profile.json` records queue depth <=17. `specimen-profiles.json` records
the existing weather durable-access, SQLite continuity, free-space14GiB and
freelist5M-page predicates. Predicate meaning is narrow: SQLite quick-check and
write acquisition do not establish application data preservation or backup recovery.
